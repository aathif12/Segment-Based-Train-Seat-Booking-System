# PostgreSQL credentials — REQUIRED, no defaults for password in production
POSTGRES_USER=lfs
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=lfsrailway

# Backend settings (automatically derived in docker-compose from the above)
# PORT=8080
# ENVIRONMENT=development
